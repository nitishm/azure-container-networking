apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base
resources:
  - deployment.yaml
  - service.yaml

secretGenerator:
- name: npm-server-certs
  options:
    disableNameSuffixHash: true
  files:
  - tls.crt
  - tls.key
